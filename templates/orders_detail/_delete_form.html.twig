<form method="post" action="{{ path('orders_detail_delete', {'id': orders_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orders_detail.id) }}">
    <button class="btn">Delete</button>
</form>
